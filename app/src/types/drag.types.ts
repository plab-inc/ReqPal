export type SortableDragItem = {
    id: string
    text: string
}
export type DragAndDropItem = {
    id: string
    top: number
    left: number
    containerId: string
    title: string
}
export type LessonBuilderDragItem = {
    name: string
}